/**
 * @deprecated
 * @file FileSystemService.ts
 * @description Этот сервисный слой является рудиментом старой архитектуры проекта.
 *
 * В новой архитектуре на Wails + Zustand его обязанности были распределены:
 * 1.  Хранение пути к директории (`rootDirectory`) теперь выполняется в `useSettingsStore.ts`
 *     с использованием `zustand/persist` для автоматического сохранения в localStorage.
 * 2.  Вызов диалога выбора директории (`SelectDirectory`) происходит напрямую из компонента
 *     `SetupDirectoryDialog.tsx`.
 * 3.  Функции для работы с файлами (`ReadFile`, `WriteFile` и будущая `ListFiles`)
 *     вызываются напрямую из компонентов, которые в них нуждаются (например, `NoteEditor.tsx`).
 *
 * Этот файл оставлен для истории, но не используется и не должен использоваться в новом коде.
 * Его можно безопасно удалить из проекта в будущем.
 */

// Код намеренно оставлен пустым.